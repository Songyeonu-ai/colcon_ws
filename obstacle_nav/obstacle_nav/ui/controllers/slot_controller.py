class SlotController:
    def __init__(self, window, ui, node, param_controller):
        self.window = window
        self.ui = ui
        self.node = node
        self.param_controller = param_controller